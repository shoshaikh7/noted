<div class="container wrapper-with-padding">
  <div class="form-header">
    <h2>Edit <%= resource_name.to_s.humanize %></h2>
  </div>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>


    <div class="form-inputs">
      <div class="input-group">
        <%= f.input_field :email, required: true, autofocus: true %>
        <%= f.label :email, "Your Email Address"%>
      </div>
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>
      <div class="input-group">
        <%= f.input_field :password, autocomplete: "off", required: false, hint: "Leave blank if you don't want to change it" %>
        <%= f.label :password, "Your Secret Password"%>
      </div>
      <div class="input-group">
        <%= f.input_field :current_password, required: true %>
        <%= f.label :current_password, "Current Password", hint: "We need your current password to confirm your changes"%>
      </div>
    </div>
    <div class="form-actions">
      <%= f.button :submit, "Update" %>
      <h3>Cancel my account</h3>

      <p>Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

      <%= link_to "Back", :back %>
    </div>
  <% end %>
</div>

<!-- FOOTER -->
<footer>
  <div class="container">
    <p>
      &copy; 2016 Shoaib Shaikh
    </p>
    <div id="logo">
      <%=  link_to image_tag("noted_logo.png").html_safe, root_path %>
    </div>
  </div>
</footer>
