%h1.title= @note.title
%p1.content= simple_format(@note.content)
%br

= link_to "Edit Note", edit_note_path
= link_to "Delete Note", note_path(@note), method: :delete, data: {confirm: "Are you sure?"}
