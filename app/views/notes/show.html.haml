-# Note
.container
  .note_show
    %h1.title= @note.title
    %hr
    .content
      %p= simple_format(@note.content)
    %hr
  .edit-btn= button_to "Edit Note", edit_note_path(@note), method: :get, class: 'btn'
