-# NOTES
.container
  .notes
    - @notes.each do |note|
      %a.note{ href: (url_for [note])}
        %h3= truncate(note.title, :length => 20)
        %h5.date= time_ago_in_words(note.created_at)

    - if @notes.blank?
      %h2 Add some notes!
      %h3 It appears you have not created any notes yet... Let's change that!

-# FOOTER
%footer
  .container
    %p &copy; 2016 Shoaib Shaikh
    #logo
      = link_to image_tag("noted_logo.png").html_safe, root_path
