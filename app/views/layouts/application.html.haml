!!!
%html

%head
  %title Noted | Never Forget
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
  = javascript_include_tag 'application', 'data-turbolinks-track' => true
  = csrf_meta_tags
  %link{:href => "https://fonts.googleapis.com/css?family=Karla:400,400italic|Poppins:400,600", :rel => "stylesheet", :type => "text/css"}

%body
  -# HEADER
  %header
    #logo
      = link_to image_tag("noted_logo.png").html_safe, root_path
    %nav
      %h3
        - if user_signed_in?
          = link_to "New Note", new_note_path
          = link_to "Edit Account", edit_user_registration_path
          = link_to "Sign Out", destroy_user_session_path, method: :delete
        - else
          = link_to "Login", new_user_session_path

  -# NOTICE/ALERTS
  - if flash[:notice]
    %div.notice
      %h4= notice
  -if flash[:alert]
    %div.alert
      %h4= alert

  = yield

-# FOOTER
%footer
  .container
    %p &copy; 2016 Shoaib Shaikh
    #logo
      = link_to image_tag("noted_logo.png").html_safe, root_path
